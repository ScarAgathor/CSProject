#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
void getUserType(int* userType);
void displayMenu(int userType);
void selectOption(int userType, int option);


int main() {
    int userType = 0;

    getUserType(&userType);
    displayMenu(userType);

    return 0;
}

void getUserType(int* userType) {
    printf("Please Enter Your User Information\n");
    printf("1 - Agent\n");
    printf("2 - Customer\n");
    printf("UserType: ");
    scanf("%d", userType);
}

void displayMenu(int userType) {
    int option;
    switch (userType)
    {
    case 1: //Agent
        printf("Hello Agent.\n");
        printf("Main Menu Options:\n");
        printf("1. Manage Routes\n");
        printf("2. Manage Vehicles\n");
        printf("3. Manage Routes\n");
        printf("Please Select an Option: ");
        scanf("%d", &option);
        selectOption(userType, option);
        break;
    case 2: //Cusomer
        printf("Hello Dear Customer.\n");
        printf("Main Menu Options:\n");
        printf("1. Book Reservations\n");
        printf("2. Book Vehicle\n");
        printf("Please Select an Option: ");
        scanf("%d", &option);
        selectOption(userType, option);
        break;
    default:
        while(userType != 1 && userType != 2) {
            printf("You Entered an Invalid option\n");
            getUserType(&userType);
            displayMenu(userType);
        }
        break;
    }
}

void selectOption(int userType, int option){
    if(userType == 1) {
        switch (option) {
        case 1:
        printf("Agent option 1");
            //call Manage Routes Function
            //shouyld caome from san antonio
            //shoyld be able to enter destination, miles, hrs
            //Should be able calculate price 
            break;
        case 2:
        printf("Agent option 2");
            //call Manage Flights Function.
            break;
        case 3:
        printf("Agent option 3");
            //call 
        default:
            printf("Invalid Option\n");
            displayMenu(userType);
            break;
        }
    }
    if(userType == 2) {
        switch (option) {
        case 1:
        printf("Customer Option 1");
            //call bookReservations Function
            break;
        case 2:
        printf("customer option 2");
            //call book Vehicles Function.
            break;
        default:
            printf("Invalid Option\n");
            displayMenu(userType);
            break;
        }
    }
}
